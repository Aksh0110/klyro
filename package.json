{
  "name": "klyro",
  "version": "1.0.0",
  "private": true,
  "description": "Klyro Multi-Vertical SaaS Platform",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspace=@klyro/config --workspace=@klyro/types --workspace=@klyro/validation --if-present && npm run build --workspace=api --workspace=web --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "pm2:dev": "pm2 start ecosystem.config.js",
    "pm2:start": "pm2 start ecosystem.config.js",
    "pm2:stop": "pm2 stop ecosystem.config.js",
    "pm2:restart": "pm2 restart ecosystem.config.js",
    "pm2:logs": "pm2 logs",
    "pm2:status": "pm2 status",
    "pm2:delete": "pm2 delete ecosystem.config.js"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
